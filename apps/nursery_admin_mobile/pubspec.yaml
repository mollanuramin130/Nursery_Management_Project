name: nursery_admin_mobile
description: GreenLeaf Operations — Admin / Staff mobile app. Consumes the same Laravel /api/v1.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.11.0
  flutter_secure_storage: ^11.0.0
  provider: ^6.1.5+1
  go_router: ^17.4.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  mobile_scanner: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
